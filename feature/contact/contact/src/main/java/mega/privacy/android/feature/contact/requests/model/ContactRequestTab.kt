package mega.privacy.android.feature.contact.requests.model

/**
 * The two tabs shown on the contact requests screen.
 */
enum class ContactRequestTab {
    /**
     * Incoming (received) contact requests.
     */
    Received,

    /**
     * Outgoing (sent) contact requests.
     */
    Sent,
}
